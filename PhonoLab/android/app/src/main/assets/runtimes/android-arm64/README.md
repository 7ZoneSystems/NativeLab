Place an executable Android arm64 llama-cli in this directory as:

    llama-cli

The app copies it into app-private PhonoLab/runtime/bin and marks it executable
on first use. Prefer the jniLibs/native-library path for release builds.
