Place a release-built Android arm64 llama.cpp executable here as:

    libllama-cli.so

The app checks Android's native library directory first and executes this file
when present. The binary is intentionally not committed.
