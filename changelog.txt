v0.3.8
1) Added MCP_SERVER block type to the pipeline builder so pipelines can call external MCP (Model Context Protocol) tools during execution.
2) Added MCP Server sidebar button, canvas color (#22d3ee cyan), icon, and legend entry in the pipeline builder.
3) Added MCP block auto-configuration dialog that opens when an MCP block is placed on the canvas, with transport selector (SSE/stdio), URL/command input, Test Connection button with green/red status dot, tool selector dropdown populated from live server response, tool description with parameter schema, and input argument name override.
4) Added `nativelab/integrations/mcp_client.py` implementing a lightweight MCP client supporting SSE (HTTP Server-Sent Events) and stdio (subprocess JSON-RPC) transports with full MCP protocol handshake (initialize, tools/list, tools/call).
5) Added MCP block execution in `PipelineExecutionWorker` that connects to the configured MCP server at runtime, calls the selected tool with the pipeline context as input, and returns the result to downstream blocks.
6) Added MCP block validation requiring both a server URL and a selected tool name before the pipeline can run.
7) Fixed pipeline validation to always run Python validation first so new block types like MCP are validated even when the native C validation layer does not recognize them.
8) Added MCP block serialization/deserialization support so MCP blocks persist through pipeline save/load with full metadata (transport, URL, tool name, argument name, connected status, discovered tools list).
9) Added MCP_SERVER to the AI Pipeline Builder allowed block types, default labels, and schema so the AI can generate pipelines containing MCP blocks.
10) Added MCP metadata documentation to the AI Builder prompt guide including transport types, URL formats, npx package syntax, tool selection, and argument mapping.
11) Added MCP metadata normalization in the AI Builder planner with safe defaults, sanitization, and transport validation.
12) Added `nativelab/pipelinebuilder/aibuilder/mcp_verify.py` implementing post-generation MCP server verification for AI-generated pipelines with auto-install, retry, and fix/remove logic.
13) Added MCP verification phase to `AiPipelineBuildWorker` that tests each MCP server after AI generation: starts stdio subprocesses, sends MCP protocol handshake, verifies tools/list response, auto-installs npm packages for npx commands, retries on failure, and reports detailed pass/fail status per block.
14) Added automatic MCP block removal for servers that fail all verification attempts, followed by AI regeneration with explicit failure context so the model can rebuild the pipeline using alternative approaches.
15) Added MCP tool list auto-update from verification results so blocks are populated with actual tools discovered from the live server.
16) Added MCP block to the pipeline canvas context menu, pill bar, and canvas rendering alongside existing block types.

v0.3.7
1) Refactored the pipeline subsystem so deterministic performance-critical work now has a native C acceleration path while preserving Python as the public orchestration and UI layer.
2) Added `nativelab/native/pipeline_core.c`, a focused native pipeline core included by the existing `_native_core` extension instead of creating a monolithic native file.
3) Added native C helpers for pipeline block ID normalization, duplicate/invalid ID repair, connection endpoint remapping, cycle detection, deterministic transform execution, merge execution, route selection, loop visit limits, and validation record execution.
4) Registered the new native pipeline helpers in `nativelab/native/_core.c` through explicit `_native_core` methods such as `pipeline_normalize_ids`, `pipeline_would_form_loop`, `pipeline_apply_transform`, `pipeline_merge_texts`, `pipeline_route_edges`, and `pipeline_validate_records`.
5) Added `nativelab/native/pipeline_core.py` as the Python wrapper/fallback layer so pipeline behavior stays available even when the native extension cannot be compiled or loaded.
6) Added `native_pipeline_available()` to the native package export surface so the app can safely detect whether pipeline acceleration is active.
7) Added `nativelab/pipelinebuilder/graph_ops.py` to centralize pipeline graph operations, including adjacency construction, block ID normalization, loop detection, and outgoing-edge routing.
8) Added `nativelab/pipelinebuilder/execution_core.py` to centralize deterministic execution helpers for transforms, merges, merge-input collection, output payload encoding, sender-label detection, reference injection, and knowledge injection.
9) Added `nativelab/pipelinebuilder/validation.py` to move pipeline validation out of the UI tab and into a reusable validation boundary backed by native records where available.
10) Refactored `PipelineCanvas` so ID repair and cycle detection delegate to the centralized graph layer instead of maintaining duplicate UI-local logic.
11) Refactored `PipelineBuilderTab._validate()` into a thin call to `validate_pipeline()` while preserving the existing user-facing validation messages and model/reference/logic/LLM guardrails.
12) Refactored `PipelineExecutionWorker` so deterministic queue transitions, route filtering, loop visit accounting, output payloads, transforms, merges, split fan-out, LLM branch routing, and context injection delegate to shared pipeline helpers.
13) Preserved Python ownership of Qt signals, UI updates, model/API calls, llama-server startup/retry handling, PDF I/O, safe Python `eval`/`exec` namespaces, LLM error propagation, and centralized error dialogs.
14) Preserved all existing safety behavior for model validity checks, LLM model attachment checks, custom-code failures, PDF dependency/file checks, filter stop output, LLM filter stop output, loop limits, duplicate connection prevention, and model-to-model connection restrictions.
15) Added optional `setup.py` build support for `nativelab.native._native_core` so source installs can build the C helpers when a compiler is available without making NativeLab unusable when native build fails.
16) Added `nativelab/native/pipeline_core.c` to `MANIFEST.in` and package data so native pipeline sources are included in source/package builds.
17) Added `tests/test_pipeline_native_core.py` covering native-wrapper parity for ID normalization, loop detection, transforms, merges, route visit limits, branch selection, graph wrapper behavior, and validation messages.
18) Expanded pipeline canvas tests to cover ID stability after the graph refactor, native-backed normalization, and validation wrapper behavior through real `PipelineBlock` objects.
19) Rebuilt the local ignored `_native_core` extension with the new pipeline C functions and verified the native pipeline wrapper loads successfully.
20) Added automatic pipeline canvas expansion for loaded JSON/example pipelines whose blocks exceed the default canvas size.
21) Added `PipelineCanvas.ensure_canvas_fits_blocks()` so the scrollable canvas grows to contain far-away loaded, added, dropped, or dragged blocks with a margin instead of clipping them outside the usable area.
22) Added explicit tracked canvas dimensions for headless/Qt-stub environments so canvas growth behavior remains testable even when `width()` and `height()` are stubbed.
23) Updated pipeline loading in `PipelineBuilderTab._replace_canvas_with_pipeline()` to normalize IDs first and then grow the canvas to fit all loaded blocks.
24) Added blank-canvas left-click hold-and-drag panning in the pipeline editor by scrolling the surrounding `QScrollArea`, while preserving existing block dragging and port-connection behavior.
25) Reset canvas size, pan state, selection, dragging, and connection state on `Clear All` so a cleared pipeline returns to the default canvas dimensions.
26) Added tests for canvas growth with far loaded blocks, reset-to-default sizing after clearing, and blank-canvas pan scrollbar movement.
27) Shrunk the main application top/menu bar slightly by reducing `QMenuBar` minimum height, vertical padding, and menu item padding in the global UI stylesheet.
28) Shrunk the main top-right menu-bar controls from `28x24` to `26x22` and lowered their container height so they no longer force the main top bar taller.
29) Kept the pipeline builder toolbar height reverted after confirming the requested shrink was for the main UI top bar, not the pipeline builder canvas bar.
30) Verified the pipeline-native migration and pipeline editor changes with focused tests for pipeline canvas IDs, native pipeline helpers, and shipped example pipelines.
31) Verified the main UI top-bar change with MainWindow split tests and compile checks for the touched UI modules.
32) Ran the full headless test suite after the pipeline/editor changes and confirmed all tests passed.
33) Added an AI Pipeline Builder panel under the pipeline builder right sidebar so a loaded model can generate NativeLab pipeline JSON from a plain-English request.
34) Added AI Builder prompt budgeting and context preflight so over-context pipeline-generation requests are blocked before model calls and explained to the user.
35) Added JSON extraction, normalization, validation, active-model placeholder filling, and one strict JSON retry for AI-generated pipelines.
36) Added AI Builder smart context with `/get_data`, `/context`, current-canvas injection, local history, and model/deterministic history compaction.
37) Added native C/Rust AI Builder helper implementations for token estimation and JSON-object span detection, with Python fallback behavior preserved.
38) Moved AI Builder into the pipeline builder's right sidebar beside the Execution tab instead of keeping it as a separate button/dialog flow.
39) Added resizable pipeline builder sidebars with live text/control scaling and retract rails so sidebars cannot collapse permanently to zero width.
40) Added compact AI Builder sidebar layout behavior so fields, buttons, history labels, and tab names remain visible when the right sidebar is narrow.
41) Added dedicated `docs/pipeline-builder.md` documentation and refreshed README/docs feature, workflow, UI, architecture, installation, models, CLI, and troubleshooting docs for the new pipeline, AI Builder, native, setup, and HF dependency behavior.
42) Added `run_pipeline_sync()` so saved pipelines can execute through the same `PipelineExecutionWorker` path from GUI, CLI, integration endpoints, and API-server routes without duplicating pipeline execution scripts.
43) Added OpenAI-compatible saved-pipeline model IDs using the `pipeline:<name>` format while preserving saved pipeline JSON loading, validation, logs, intermediate output handling, and existing block behavior.
44) Added `/v1/models` to the integration HTTP endpoint so external tools can discover saved visual pipelines as OpenAI-style model records.
45) Added `/v1/chat/completions` and `/chat/completions` to the integration HTTP endpoint so `model: "pipeline:<name>"` runs the selected saved pipeline through the normal executor.
46) Added structured OpenAI-style error payloads for pipeline-model execution failures, including missing pipeline, unknown model, unsupported streaming, and executor errors.
47) Refactored CLI pipeline execution to reuse `run_pipeline_sync()` so CLI and API pipeline runs stay behaviorally aligned with the GUI pipeline executor.
48) Added saved-pipeline entries to the Dev > API Server model catalog so the **Hosted model** dropdown can select pipelines alongside active/local/API-backed models.
49) Updated the Dev > API Server runtime so a selected or requested `pipeline:<name>` bypasses model loading and routes directly through the existing pipeline executor instead of launching a separate runner.
50) Kept API Server OpenAI and Anthropic protocol behavior intact for pipeline refs, including normal auth, bind/port settings, `/v1/models`, `/v1/chat/completions`, `/v1/completions`, and `/v1/messages` handling.
51) Added tests covering integration pipeline model listing/execution and API Server hosted-pipeline catalog/execution behavior.
52) Updated integration, feature, UI, pipeline, troubleshooting, and workflow documentation for saved pipelines as API models and Dev > API Server hosted-pipeline selection.
53) Smoke-tested the live integration endpoint at `http://127.0.0.1:8765`, confirming `/health`, `/snapshot`, `/pipelines`, and `/v1/models` return successfully with four saved pipeline models.
54) Verified missing pipeline execution returns the expected `404 pipeline_not_found` structured response instead of crashing or silently falling back.
55) Re-ran the full headless test suite after the API model work and confirmed all 66 tests passed.
56) Moved the canonical saved-pipeline storage path from `~/.native_lab/pipelines` to `localllm/pipelines` so user-created pipelines stay with the rest of NativeLab's local app data.
57) Added a compatibility migration that copies existing legacy `~/.native_lab/pipelines/*.json` files into `localllm/pipelines` without overwriting newer files.
58) Updated CLI pipeline listing/loading and data-portability imports so saved pipelines consistently resolve to `localllm/pipelines`.
59) Added pipeline-name sanitization for save, load, delete, CLI, integration, and API-server paths so path-like names cannot escape the configured pipeline directory.
60) Hardened legacy pipeline migration to skip symlinks and non-files while copying old pipeline JSON into `localllm/pipelines`.
61) Added request body size and malformed JSON guards to the API Server and integration HTTP bridge so oversized or invalid POST requests return structured 413/400 errors instead of reading unbounded input.
62) Hardened API Server authentication checks with constant-time key comparison and a reduced unauthenticated health payload when API keys are required.
63) Changed the API Server key reveal UI from one global **Show keys** toggle to per-key eye buttons beside the localhost and WiFi/LAN API key fields.
64) Added regression tests covering pipeline storage path safety, legacy symlink migration skipping, missing pipeline-model rejection, and protected health payload behavior.
65) Hardened API Server tab construction and shared toggle controls for headless/no-GUI compatibility so UI modules can still import and smoke-test safely in CLI/test contexts.
66) Replaced the API Server key reveal icon with a plain Lucide eye SVG asset and aligned each Generate button inside its matching API-key input row.

v0.3.6
1) Added a first-run automatic setup pipeline that profiles the user's OS, available/used RAM, CPU threads, GPU backend, VRAM, and accelerator type before choosing a practical starter model.
2) Added auto-setup backend choice in the app prompt so new users can pick llama.cpp GGUF easy mode or Hugging Face Transformers hard mode before setup begins.
3) Added persistent auto-setup state under `localllm` so setup is resumable after pause, cancel, crash, or app restart instead of starting from zero.
4) Added pause, resume, cancel, and progress handling for first-run setup, including preserved partial downloads and a resumed-state prompt.
5) Added hardware-aware model selection catalogs for low-end CPU-only machines through higher-end CUDA/ROCm/Vulkan-capable machines, with separate model choices for llama.cpp and HF Transformers.
6) Added llama.cpp runtime auto-selection logic that chooses a matching runtime build for CPU, CUDA, Vulkan, ROCm, or platform-specific fallback where possible.
7) Added safer llama.cpp release/build fetching and download handling through existing runtime download subsystems instead of duplicating model/runtime storage paths.
8) Added Hugging Face snapshot discovery and download support to auto-setup for HF Transformers models, using the existing HF snapshot downloader and registration format.
9) Added safer path handling for downloaded HF snapshot files so repository paths cannot escape the configured destination directory.
10) Added resumable `.part` download behavior for llama.cpp and HF snapshot/runtime downloads so interrupted downloads can continue later.
11) Added edge-case normalization for stale or partial auto-setup state, including backend aliases, resumable stages, and incomplete saved state recovery.
12) Added tests covering auto-setup hardware planning, backend normalization, runtime asset selection, model choice, GGUF quant selection, and safe child download paths.
13) Centralized real context reporting for the bottom bar so chat, Labs, edit flows, and other LLM activity can report actual active context instead of only chat-local context.
14) Added centralized context-limit display updates and real-time context meter refresh hooks across LLM-driven workflows.
15) Added an in-app Hugging Face Transformers library installer inside the HF Transformers downloader card so users who only downloaded NativeLab can install runtime libraries without opening a terminal.
16) Added shared HF dependency metadata in `nativelab/Server/hf_deps.py`, including explicit package requirements, import checks, dependency status reporting, and pip command generation.
17) Added a cancellable HF dependency install worker that runs `python -m pip install -U ...`, streams output to the UI, and verifies imports after pip exits.
18) Added Check, Install Libraries, Cancel, and live log controls to the Download tab's HF Transformers snapshot section.
19) Updated HF backend missing-library errors so users are directed to the new in-app installer as well as the manual pip command.
20) Added tests for HF dependency command generation, dependency registry shape, and runtime import status reporting.
21) Split the old 4000-line `nativelab/main.py` into a small GUI entrypoint plus focused MainWindow modules under `nativelab/UI/mainwindow`.
22) Moved MainWindow construction to `nativelab/UI/mainwindow/window.py` and divided behavior into feature mixins for auto setup, context controls, UI building, models, sessions, engine runtime, Labs, chat/pipeline, documents, and status/view/theme handling.
23) Moved GUI loader threads into `nativelab/core/model_loaders.py` so model/API loading workers no longer live inside the entrypoint.
24) Added `nativelab/UI/mainwindow/shared.py` to centralize imports used by the split MainWindow modules while preserving existing behavior during the refactor.
25) Reduced `nativelab/main.py` to CLI-safe GUI startup, QApplication setup, font/icon setup, SIGINT handling, and MainWindow launch.
26) Updated architecture documentation to describe the new GUI entrypoint, MainWindow package, model loader module, and Qt worker lifecycle helper.
27) Fixed the MainWindow mixin method-resolution order after the split so `eventFilter` and `closeEvent` correctly resolve through `QMainWindow`.
28) Added defensive fallbacks around `eventFilter` and `closeEvent` so headless stubs or unusual Qt bindings do not crash if a parent method is absent.
29) Restored split-time details that were easy to lose during mechanical extraction, including `_CODING_KEYWORDS` and `_vline` as a static helper.
30) Added regression tests for the MainWindow split, including MRO order, safe `eventFilter` fallback, class constants, and static helper behavior.
31) Patched `nativelab/api_server/tab.py` to use NativeLab's Qt compatibility layer instead of direct PyQt imports, keeping headless imports stable.
32) Added `nativelab/UI/qt_workers.py` to centralize QThread shutdown, signal disconnection, stuck-worker detection, and safe worker attribute cleanup.
33) Reworked MainWindow shutdown to stop active generation, pipeline, chat-pipeline, summary, multi-PDF, auto-setup, model loader, API loader, role loader, and child-tab workers through the centralized helper.
34) Changed shutdown behavior so NativeLab warns and ignores close if a non-cancellable worker is still running instead of deleting Qt widgets underneath live workers.
35) Added shutdown hooks for Accounts and Hugging Face login workers so token validation/login workers are stopped before widgets are deleted.
36) Added shutdown handling for the Download tab covering GGUF search, GGUF download, llama.cpp release fetch/download, HF snapshot search/download, HF dependency install, Ollama listing, and Ollama pull workers.
37) Preserved partial download files during shutdown by using non-destructive worker aborts for model/runtime downloads.
38) Hardened theme switching so Accounts and Download widgets run shutdown hooks before being deleted and rebuilt.
39) Replaced several ad-hoc `abort(); wait()` paths with centralized worker shutdown calls in context reload, Stop, document summary, Labs model reload, and role-loader cleanup flows.
40) Added tests for Qt worker lifecycle helpers, including abort/wait/disconnect behavior and stuck-worker reference preservation.
41) Verified the refactor and hardening with headless compile checks, unit tests, MainWindow import checks, real PyQt offscreen construction, theme toggle, close handling, and tab-level shutdown smoke tests.
42) Added optional native C backend helpers under `nativelab/native/_core.c` for deterministic hot-path backend work while keeping Python as the public wrapper surface.
43) Added C-side prompt construction for text chat prompts so llama.cpp prompt assembly can run through native code when `_native_core` is available.
44) Added C-side sampler normalization for Top-K, Min-P, Typical-P, and Seed so llama.cpp server, llama-cli, Ollama, and HF Transformers settings share one validated payload shape.
45) Added C-side CLI sampler argument appending so llama-cli command construction can reuse the same normalized sampler rules as server/API-style backends.
46) Added C-side context-window error detection for common llama.cpp/Ollama/HF context overflow messages, with Python fallback behavior preserved.
47) Added C-side reference chunk splitting for repeated reference/context preparation work while keeping the existing Python fallback path for unsupported builds.
48) Added `nativelab/native/engine_helpers.py` as the Python wrapper layer for native prompt building, sampler payloads, CLI sampler args, Ollama/HF sampler mapping, image-base64 extraction, context-error checks, and reference chunking.
49) Added optional Rust model-detection helpers through `nativelab/native/rust_model.rs` and `rust_model.py` for GGUF model family detection and quantization detection.
50) Wired Rust-backed family and quant detection into `Model/model_family.py`, falling back to the existing Python pattern matcher when the Rust shared library is unavailable.
51) Added native availability checks (`native_core_available()` and `rust_model_available()`) so the app can safely use C/Rust acceleration opportunistically without making native builds mandatory.
52) Preserved plugin, UI, and backend orchestration in Python while moving only deterministic backend helper paths into C/Rust acceleration layers.

v0.3.5
1) Diagnosed the py-to-doc crash path around manual model reloads and local llama.cpp server lifecycle during documentation generation.
2) Added smarter py-to-doc auto-reload routing through LabEndpoints so model/context reload requests go through the host app instead of each lab touching engines directly.
3) Added py-to-doc auto model reload controls for low-RAM recovery, reload thresholds, re-arm margins, minimum step/time spacing, and failure limits.
4) Added py-to-doc abrupt-failure detection for saved project jobs so stale non-paused tasks can be marked paused/recoverable after crashes.
5) Improved py-to-doc resume handling so interrupted jobs can recover from saved task JSON/checkpoints instead of being treated as dead runs.
6) Restored proper local model unload behavior so unloading a model tears down/restarts the underlying server state instead of only updating GUI status.
7) Centralized engine status in `nativelab/core/engine_status.py` so chat, Labs, pipeline, model loading, CLI, and API surfaces share one status detector.
8) Rewired LabEndpoints, LlamaEngine, API engine status, Code Edit, py-to-doc, pipeline status badges, and main-window status updates to use the centralized engine status snapshot.
9) Updated the comparison/main site with current NativeLab features and made comparison actions visible as first-class buttons instead of hidden top-only options.
10) Added editable model profile parameters for downloaded/local models, Ollama models, and HF Transformers models from the Models tab.
11) Added an Advanced model-profile dropdown with parameter descriptions beside each setting for context, threads, sampling, seed, and backend-specific options.
12) Centralized saved model profiles so loading the same model from Chat, Models, CLI, Labs, integrations, or API-server paths reuses the profile saved in Models.
13) Wired saved sampler/profile settings through local llama.cpp server calls, llama-cli calls, Ollama calls, and HF Transformers generation.
14) Added Settings > Accounts data export for full NativeLab app data from `localllm` as one portable file selected through a save dialog.
15) Added export category selection with all categories checked by default, including pipelines, chat history, API model data, local model profiles, Discord bot data, WhatsApp bot data, py-to-doc jobs, and other app data.
16) Added data import wizard support that reads an export file, shows importable categories/files with checkboxes checked by default, and restores selected JSON/data back into normal `localllm` storage.
17) Added centralized data portability logic in `nativelab/core/data_portability.py` and wired import refresh into the main app after restoring data.
18) Added Dev > API Server tab for locally hosting downloaded models or active NativeLab engine models over HTTP endpoints.
19) Added separate local and LAN/global API-server endpoint modes with separately bound API keys.
20) Added OpenAI-compatible `/v1` routes for models and chat completions.
21) Added Anthropic-compatible routes for model/message access.
22) Added API-server model catalog/capability reporting so available models expose backend type, text/image capability, and model source.
23) Split API-server implementation into boundary modules for config, catalog, protocol translation, server runtime, and GUI tab.
24) Verified the local API server against the configured `127.0.0.1:8787/v1` endpoint and generated local API key.
25) Added API-server config persistence in `localllm/api_server_config.json`.
26) Added app shutdown cleanup for active API servers, integration HTTP endpoints, Discord/WhatsApp bot runners, MCP subprocesses, and local engine workers.
27) Added hide/show toggle for generated API keys in the API Server window so users can reveal keys when needed.
28) Added safer API-server shutdown handling from the main window close event.
29) Separated CLI and GUI startup paths so `nativelab --cli` no longer initializes PyQt or GUI-only modules.
30) Changed the console script entrypoint to route through `nativelab.__main__` instead of importing `nativelab.main` directly.
31) Added `NATIVELAB_CLI`/`NATIVELAB_NO_GUI` routing flags before CLI imports so shared modules know they are running headless.
32) Added `nativelab/imports/qt_compat.py`, a headless Qt compatibility layer for shared CLI-safe workers/signals without importing PyQt.
33) Updated shared imports, LabEndpoints, HF auth, UI helpers, pipeline flow preview, and CLI lab event loops to use the Qt compatibility boundary.
34) Made the Labs package lazy so importing LabEndpoints in CLI runtime does not import GUI lab panels.
35) Added a defensive early CLI route in `nativelab/main.py` so stale generated wrappers that still import `nativelab.main` route to CLI before GUI imports.
36) Removed noisy import-time llamaengine debug printing while preserving model load logging.
37) Verified `nativelab --cli status`, CLI model listing, CLI Labs metadata, and py-to-doc/code-edit helper imports run with no `PyQt6` modules loaded.

v0.3.4

1) Fixed dependency vulnerability alerts by regenerating `uv.lock` with secure package versions.
2) Replaced deprecated `PyPDF2` usage with maintained `pypdf` for PDF reading and summarization paths.
3) Updated locked packages for security: `urllib3`, `Pillow`, `filelock`, `requests`, `transformers`, and `pypdf`.
4) Raised the supported Python baseline to 3.10+ so vulnerable Python 3.9 resolver branches are no longer included in the lockfile.
5) Updated PDF dependency install messages and documentation from `PyPDF2` to `pypdf`.

v0.3.3

1) Added compact IDE-style UI spacing across tabs, panels, controls, chat widgets, and labs screens.
2) Reduced the bottom bar height and tightened broad tab/sidebar spacing for a denser workspace layout.
3) Fixed remaining brown/previous-theme borders and button colors after switching between light and dark modes.
4) Added adaptive PyQt window sizing so sub-windows scale better across different screen resolutions.
5) Replaced remaining checkbox-style boolean controls with moving toggle controls and fixed dark/light toggle handle colors and responsiveness.
6) Added original Hugging Face and Ollama SVG assets and kept brand icons from being recolored by theme updates.
7) Fixed Labs, Structured Edit, and Py-to-Doc SVG icon colors so they remain readable without forced white recoloring.
8) Updated dropdown/menu styling in Hugging Face, Ollama, and related settings areas to use the active system/theme palette.
9) Removed the hard response-output word cap for API models so provider generations no longer stop abruptly because of the app cap.
10) Added offline KaTeX-based TeX/LaTeX rendering in shared output renderers, with local scripts, CSS, fonts, and license packaged in the app.
11) Added mixed Markdown + LaTeX rendering support, including inline math, display math, common math environments, fenced-code protection, and Markdown table rendering with formulas inside cells.

v0.3.2

1) Fixed error surface in labs endpoints to show real endpoint errors instead of only HTTP 400 text.
2) Added py-to-doc project pause/resume checkpoints in localllm/temp for restart and shutdown recovery.
3) Moved Labs, Logs, Integrations, Pipeline, and MCP under a Dev tab with a contextual left sidebar, and moved Config to the top-right settings button.
4) Added Dev > Skills with a shared localllm skill library, /skills endpoint, and chat toggle for skill injection across chat, labs, and integrations.
5) Added built-in edit skill and structured-edit lab for temp-backed iterative code edits before saving to source files.
6) Added live structured-edit response capture and colored + / - line counts for each model edit.
7) Upgraded structured-edit into a chat-style edit workspace with saved checkpoints, branch-from-history, editable responses, clickable per-turn diffs, live file preview, and Python diagnostics.
8) Added Python diagnostics in Code Edit with syntax checking, lightweight warnings, a Problems panel, and editor line highlighting.
9) Moved detailed Code Edit diffs into per-message chat cards so users can inspect changes for every prompt in the conversation.
10) Fixed Dev tab and Dev sub-tab heading colors so they update correctly when switching dark/light themes.
11) Added independent scrolling to the Code Edit file sidebar so diagnostics, structure, logs, and the editor do not overlap on small screens.
12) Upgraded `nativelab --cli` into a terminal control center with Chat, Models, API Models, Skills, Labs, Pipelines, Integrations, Status, Setup, and Quit.
13) Added shared CLI runtime wiring so chat, Labs, pipelines, endpoint serving, integrations, local GGUF models, API models, and skill injection use the same backend surface.
14) Added CLI commands for models, api-models, skills, labs, pipeline, integrations, endpoint, and serve, with `--json` on inspect/list/show flows.
15) Expanded CLI chat slash commands with `/models`, `/api-models`, `/skills`, `/pipelines`, `/pipeline`, `/labs`, `/py-to-doc`, `/code-edit`, `/endpoint`, and `/serve`.
16) Added CLI model/API management for listing, loading, and setting defaults in `localllm/cli_prefs.json`.
17) Added CLI skill create/edit/delete/enable/disable controls and a saved chat skill toggle.
18) Added CLI Labs support for py-to-doc single/queue/project modes and temp-backed structured code-edit sessions with terminal diffs, save/save-as, and optional editable model JSON responses.
19) Added CLI saved pipeline list/show/run support and foreground integration endpoint serving.
20) Added CLI Discord/WhatsApp profile list/show/create/edit/delete/run commands with redacted secret display.
21) Refreshed CLI docs, beginner guide, README CLI section, and CLI smoke paths for clone/package usage.
22) Enhanced py-to-doc project mode with recursive .gitignore-aware Python discovery and pre-created mirrored output directory structure.
23) Fixed py-to-doc class method discovery to document every direct class method, repair incomplete completed checkpoints, and added explicit project resume controls in GUI and CLI.
24) Expanded explicit server, model-loading, inference, labs, pipeline, download, subprocess, and integration bot timeouts to 24 hours.
25) Added py-to-doc context policies with no-reset, fixed-reset, and auto budget reset modes plus GUI and CLI controls.
26) Synced py-to-doc auto context budgets with local llama.cpp reloads and raised app context caps to 300k tokens.
27) Added a py-to-doc saved project task card that lists checkpoints and resumes selected jobs automatically.
28) Added endpoint-level context-exceeded errors and py-to-doc same-section retry after clearing/reloading context.
29) Added Ollama backend refs (`ollama:<model>`) behind the shared local engine so chat, labs, pipelines, integrations, CLI, and status widgets can use running Ollama models.
30) Added optional Hugging Face Transformers backend refs (`hf:<repo-or-dir>`) with safetensors/local-dir support and image-text model routing where Transformers supports the architecture.
31) Added Models-tab controls for registering Ollama and HF models, plus CLI/backend ref loading for `ollama:` and `hf:` targets.
32) Added Download-tab HF Transformers snapshot downloads with repo inspection, revision support, preserved subdirectories, `.part` resume, pause/cancel controls, token auth, and auto-registration as `hf:<local-folder>`.
33) Added Download-tab Ollama model pulls through an already-running Ollama daemon with installed-model listing, streamed `/api/pull` progress, configurable host, and auto-registration as `ollama:<model>`.
34) Added editable HF Transformers and Ollama backend settings in App Configuration, including HF cache/download dir, token, revision, trust remote code, local-files-only, safetensors, dtype, device map, low-CPU-memory load, attention implementation, max-memory map, quantization, Ollama host, and Ollama keep-alive.
35) Wired `LlamaEngine` HF/Ollama loading and generation to read saved backend settings so chat, Labs, pipelines, integrations, CLI-visible status, and model-role loading share the same runtime behavior.
36) Added curated popular model presets in `templates.py` for GGUF repos, HF Transformers snapshots, and Ollama library models, and wired them into the Download tab as repo/model pickers.
37) Added a top-level Accounts tab with a Hugging Face sub-tab for one-click browser/device OAuth using NativeLab's public client ID, local credential storage in `localllm/cred/huggingface.json`, saved-token reuse for GGUF/HF downloads and `hf:` model loading, an advanced manual-token fallback, masked auth display, and clearer 401/403 login/access errors.
38) Improved gated Hugging Face repo failures so signed-in 403 responses explain that model-specific access may still need acceptance or approval instead of incorrectly asking the user to sign in again.
39) Added py-to-doc auto-mode RAM-triggered local model reloads between files and endpoint-only model reload/wait routing for Labs.
40) Improved Ollama connection handling across downloads, model loading, and chat so connection-refused errors explain that the Ollama daemon must be started or the host setting updated.
41) Reworked Settings into a sidebar with General, Docs, Hugging Face, Ollama, Server, Appearance, and Accounts pages, and hid Dev behind a Developer Mode toggle.

v0.3.0
1) Added que management for py to doc [labs feature]
2) Blackbox api key issue solved with log handler 
3) Fixed issue where api models didnt came as seperate models nd glitched wiyh local models
4) Added image input support for api and supported local models 
5) Added vlm and vision support through llama.cpp and auto detection 
6) Added katex and LaTeX support for md to html works nd chat bubbles
7) Added ui sidebar controls , tab controls
8) Replaced emojis with corresponding svgs
9) Ui chat area improved , no auto load of default model

v0.2.9 [major update]
1) Added Integrations tab with endpoint browser and local HTTP bridge.
2) Added integration routes for runtime, models, API models, limits, saved pipelines, labs, py_to_doc, call_llm, and saved Discord/WhatsApp bot profiles.
3) Added Discord bot connector profiles saved in localllm/integrations/discord_bots.json.
4) Added Discord bot start/stop controls inside the app with live bot logs.
5) Added Discord slash commands for help, ask, status, pipelines, pipeline details, labs, lab details, and models.
6) Added optional direct @Bot mention replies with Message Content Intent support.
7) Added Discord request queue controls, reply mode controls, permissions, invite URL helper, and NativeLab access toggles.
8) Added integration docs and packaged Discord connector runtime/example assets.
9) Kept model loading manual-first: app does not auto-load default models until user action.
10) Added WhatsApp Cloud API connector profiles with local webhook run/stop controls, logs, queue controls, command access, and docs.
